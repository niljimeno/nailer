Git repository manager backend - early stages

To run it in your pc, use docker-compose:

```bash
docker-compose up --build
```

To visualize the project capabilities, go to `http://localhost:8080/swagger-ui/index.html` while docker compose is running.
